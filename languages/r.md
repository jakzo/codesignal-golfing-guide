# R

## Function Declaration
``` r
convertString = function(x, y, z) x + y + z

convertString = function(x, y, z) {
  x + y + z
}
```

## Logging
``` r
convertString = function(x, y, z) {
  print(x*y, y*z)
  x + y + z
}
```
