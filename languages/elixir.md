# Elixir

## Function Declaration
``` elixir
def challenge(x, y, z), do: x + y + z

def challenge x, y, z do
  x + y + z
end
```

## Logging
``` elixir
def challenge x, y, z do
  # TODO
  x + y + z
end
```

## Import
``` elixir
# String.a() <> String.b()
import String
a() <> b()
```
